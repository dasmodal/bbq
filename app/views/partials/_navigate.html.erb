<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BBQ</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Профиль', current_user, class: 'nav-link actice' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Выйти', destroy_user_session_path, method: :delete, class: 'nav-link actice' %>
          </li>
        <% else %>
          <%= link_to 'Войти', new_user_session_path, class: 'nav-link actice' %>
          <%= link_to 'Зарегистрироваться', new_user_registration_path, class: 'nav-link actice' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
